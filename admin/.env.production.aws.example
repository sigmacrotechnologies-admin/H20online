# ═══════════════════════════════════════════════════════════════════════════
# H2O Online — ADMIN production build (AWS EC2)
# ═══════════════════════════════════════════════════════════════════════════
# Copy to admin/.env.production before building on server or locally for deploy:
#   cp .env.production.aws.example .env.production
# Then: npm run build:prod
# ⚠ CHANGE FOR PRODUCTION if EC2 IP or domain changes (must match config/aws-production.json)
# ═══════════════════════════════════════════════════════════════════════════

VITE_API_URL=http://13.62.57.255:5000

# With HTTPS + domain later (rebuild admin after change):
# VITE_API_URL=https://api.yourdomain.com
