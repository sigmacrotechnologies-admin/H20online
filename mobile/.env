# Backend URL: use your PC's LAN IP (Expo shows it when you run npm start)
# Android emulator: 10.0.2.2 = host machine. Physical device: use IP from Expo (e.g. 192.168.1.13)
EXPO_PUBLIC_API_URL=http://192.168.1.2:5000
