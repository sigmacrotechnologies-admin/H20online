# Backend URL: your PC IP so the phone can reach the backend. Get it: ipconfig (use the 192.168.x.x from Wi-Fi).
# After changing this, run: npx expo start -c
# Android emulator only: use http://10.0.2.2:5000
EXPO_PUBLIC_API_URL=http://172.20.10.5:5000
